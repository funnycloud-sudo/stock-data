name: Test schedule

on:
  schedule:
    - cron: '*/15 * * * *'
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Mostrar hora UTC
        run: |
          echo "UTC:"
          date -u
          echo "Runner local:"
          date
